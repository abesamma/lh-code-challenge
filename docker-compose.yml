services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: lh-code-challenge:latest
    ports:
      - "3000:3000"
    environment:
      PORT: "3000"
      SQLITE_DB_PATH: /app/data/app.db
    volumes:
      - sqlite_data:/app/data

volumes:
  sqlite_data: